# family-flick-coordinator
A simple web application that families and friend groups can use to schedule group movies, based particularly on who is interested and when.

This app is still in development. Since it requires Python libraries, two setup scripts (`setup.bat` for windows and `setup.sh` for Mac/Linux) are included. Navigate to the repository folder and run the respective script after cloning the repository, and the virtual environment should set itself up.